# 预承诺页重判（任务六点五 7c，2026-08-24）

分支二修复循环后的重判，取代 20260824-202126/trigger_decision.md 的
分支二结论；判定数据见同目录 metrics.json 与
results/description/entropy_repair/20260824-205638/summary.json。

## 修复内容与证据（分支二关闭）

1. 匹配聚簇真值（对 b1_s0 / b4_s0 末检查点潜态拟合 GMM，BIC 选 K=32，
   真值熵 MC 精度 ±0.002）上两路误差定量：两路同向上偏——flow 加强档
   （12 耦合 / 128 隐宽 / 25 轮）+0.62~0.70，kNN（标准化，k=1）
   +0.26~0.32；偏差之差 ≈0.37 定量解释了此前实测的双路分歧方向与
   量级——分歧是系统偏差之差，非随机故障。
2. 绝对偏移的模板校准不跨 run 迁移（b1_s3 末检查点分歧仍达 2.0 nats；
   机制：dSprites 因子网格的近离散结构使绝对熵对后验宽度尺度敏感，
   两路平滑行为不同）。
3. 判据操作化修正：P1 判读用 Ŝ 差分与形状，稳定的分布依赖偏差在
   差分中相消。实测形状一致性（10 run 全体）：整条曲线秩相关
   0.965–1.000，均匀段差分相关 0.943–1.000，末窗 ΔŜ 全部同号。
   双路判据改为"逃逸 run 秩相关 ≥ 0.9 且末窗 ΔŜ 同号"（阈值提案，
   冻结时定；实测最小 0.965 留余量）；绝对分歧与幅度差降为诊断量。
   口径定案：Ŝ 主口径 = flow 加强档（偏差特征已在匹配真值上量化），
   kNN（标准化 k=1）为形状互证。

## 重判结果：三分支均不触发

- 分支一：判据 2（S2 结构）通过、判据 3（双路一致，形状口径）通过；
  判据 1（效用平台 ≥3/5 种子）失败——0/10 检出，数据条件满足下的
  曲线性质（峰后缓降）。
- 分支二：双路一致性已修复通过；S1 压缩签名未触发（漂移 10–14%）。
- 分支三：不触发（P2 分离 0.97–1.00 远超空白带）。

## 页外情形，停下呈报

预承诺页未预设"仅效用平台前提失败、其余全部健康"的格局。这不再是
仪器问题（仪器验收全部通过），是 P1（能量下降与拟合后压缩）的检验
前提在本 S1 配置上的实测性质问题，处理属协议层决定，报用户定夺后
才能进入任务七（冻结）。备选路径见任务回报。
