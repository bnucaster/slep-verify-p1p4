# CLAUDE.md · SLEP P1–P4 实证检验：项目规则

## 项目

检验母论文《The Semantic Least-Energy Principle》4.7 节（预测协议）的四条预注册预测：P1（能量随学习下降、拟合后压缩）、P2（推理轨迹集中于低作用量路径）、P3（低梯度区审议轨迹近测地）、P4（自信息对势的仿射律与温度可辨识）。执行协议的唯一权威是 docs/plan_v2.md（综合实验方案 v2）；本文件是执行纪律。两者冲突时停下报告用户，不自行裁决。母论文 PDF 与预算 proposal 在 docs/。

## 硬规则（违反任何一条即停止当前任务并报告）

1. 数据分族。校准种子 {99, 100}，开发族种子 0–4，评估族种子 5–9（configs/seeds.yaml）。docs/freeze_status.json 的 frozen 置位之前，评估族禁止进入任何训练、加载、读取或分析路径；src/slep/guard.py 机械强制，禁止绕过、注释、弱化或在新代码路径中遗漏守卫调用。
2. 阈值溯源。判定阈值不得手拍；代码中的阈值常量必须带注释指向 results/calibration/ 下的推导产物文件。校准完成前一律写"待校准"占位并注明来源缺口。
3. 判定只读。results/confirmation/metrics.json 只能由冻结后的 judge 代码写入；人与助手都不得手改其中判定字段。
4. 双时间尺度。采推理轨迹前模型参数必须冻结，配置里显式记录冻结来源检查点路径。
5. 三值记账。每条预测 × 每个系统的判定取 通过 / 失败 / 未构成检验；未过入域门记第三值，禁止与失败合并统计。
6. 结果纪律。汇报中的一切实验数字只能来自实际运行的产物文件；运行失败就报失败与原因；禁止以预期值、示例值或外推值冒充实测。

## 规则一 · 任务后进度回报

每完成一个任务：先更新 PROGRESS.md，再在回复末尾按以下格式回报。

- 分阶段进度：当前阶段名与出口条件、完成百分比，依据须指向具体产物文件。
- 综合进度：按权重合成。权重：M1 校准 15%、M2 描述 20%、冻结与预注册 5%、确证 50%（其中 EXP-P4 仿射律 15%、EXP-P1 能量下降 10%、EXP-P3 近测地 12%、EXP-P2 低作用量 13%）、M6 写作 10%。权重为约定，冻结前可与用户商定调整，冻结后不动。
- 健康度：绿 / 黄 / 红。红：发生协议违规（评估族污染、手改判定、手拍阈值），或仪器门失败阻塞超两周。黄：存在未解决的估计器或仪器门问题、进度落后计划两至四周、或预算消耗超同期计划三成。绿：以上皆无。
- 信心：高 / 中 / 低，指"当前阶段能满足出口条件"的判断，必须附依据；依据须区分事实与推断；依据不足就写明不确定的具体是哪部分。

## 规则二 · 语言禁忌（适用于全部输出、文档与代码注释）

【语言禁忌】
- 禁用"不是……而是……""不仅……更是……"等否定对比句式；有观点直接陈述。
- 不用排比，不凑三项；列举按实际数量来，允许长短不一。
- 默认不打比方。只有概念确实抽象、且一个类比能省掉大段解释时才用，全文尽量不超过两处。
- 删掉铺垫、拔高和结尾升华。禁用"值得注意的是""综上所述""首先/其次/最后""赋能""闭环"等套话。
- 少用破折号、加粗、emoji；不堆形容词，不写"从X到Y"式空泛概括。

【代号与指代】
- 使用任何代号或编号——如"假设1""P1""阶段2""方法A""定理3""公式(2)"——首次出现时必须说明其内容；之后每次引用都附上几个字的括号提示（如"阶段2（指令微调）"），不能只甩编号让读者往回翻。
- 自己行文时少造代号：对象只有两三个就用有意义的名字（如"基线模型/蒸馏版"），不用"方法A/方法B"。
- 代词（它/这/该方法）指代必须无歧义，相隔一段以上就重新点名。

【数学公式】
- 每个公式首次出现的全部符号——变量、常量、上下标、希腊字母、函数与算子、集合——必须紧跟公式逐一解释，包括含义和维度/取值范围。
- 同一符号全文含义一致，不得中途换记号；已解释过的符号不重复解释。
- 每个公式之后用自然语言说明其现实意义：它在计算什么、为什么这样设计、各项直观上对应什么。
- 不确定原论文的公式或记号时，禁止凭记忆默写，标注[待核实]或改用文字描述，不得编一个形似的。
- 长推导只保留关键步骤并说明每步目的，纯代数变形省略。

【事实纪律】
- 只陈述有把握的内容。不确定就直说，并指明不确定的具体是哪部分。
- 严禁虚构数据、日期、人名、引文、文献、链接。查不到就标注[待核实]。
- 区分并标明：确认的事实 / 有依据的推断 / 猜测，推断给出依据。
- 改写、润色我提供的文字时，不得添加原文没有的事实、细节和情绪，不得改动数字和关键限定词。
- 涉及可能过时的信息（版本、指标、人事）先核实，无法核实就注明时效风险。

## 工程约定

- Python 3.11+，PyTorch；实验配置 yaml 化；种子只从 configs/seeds.yaml 读取，不得内联。
- 每次运行在 results/<阶段>/<实验>/<run_id>/ 落盘：配置快照、git commit hash、日志与产物。
- 估计器自检写成单元测试：线性闭式对拍、合成熵恢复、合成 Langevin 恢复。新估计器先过自检再接入实验。
- 长任务拆成可断点续跑的小步；每步产物落盘后再进下一步。
- 新增依赖登记进 pyproject.toml 并注明用途。
- 提交信息注明所属阶段与任务编号，便于协议审计。

## 目录地图

- src/slep/estimators/：度量 ĝ、势 V̂、熵与密度、自信息 Î（含体积校正）、OM 作用量 Â_OM、测地求解与偏差、经验漂移分解、支撑度诊断
- src/slep/systems/：S1（β-VAE / dSprites）、S2（GRU 世界模型 / 网格世界）、S3（小 Transformer 最小版）、合成校准系统
- src/slep/protocols/：入域门、平台检测器、四实验协议、聚合与 judge
- src/slep/guard.py：评估族隔离守卫
- configs/：seeds.yaml 与实验配置
- docs/：plan_v2.md（协议权威）、proposal.md、precommitment_template.md（预承诺页）、protocol_v1.1_draft.md（预注册草稿）、freeze_status.json、initial_prompt.md
- results/：calibration / description / confirmation 三阶段产物
- PROGRESS.md：进度板

## 当前状态

阶段 M1（校准）。入口任务见 docs/initial_prompt.md。
