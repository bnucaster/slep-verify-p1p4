# 预承诺页（M2 描述阶段开始前填写并提交入库）

作用：框住探索阶段的结论空间，防止事后任何图都被读成支持。触发判定在 M2（描述）结束时执行并记入 PROGRESS.md 事件日志。

填写说明：判据中的"噪声带 / 地板 / 容差"一律指 results/calibration/thresholds_v1.json 及其溯源产物中的量，或描述阶段按预定公式从开发族种子间方差现算的量；不引入本页之外的手拍数。三分支互斥时按一 > 二 > 三的顺序取先触发者；分支二触发后修复重测，再回本页重判。

## 分支一 · 转预注册

触发格局（同时满足以下三条）：

1. S1（β-VAE）：至少一个 β 档在 ≥3/5 个开发种子上，冻结版平台检测器
   （protocols/plateau.py，W 按 CAL-P1 表配实测 σ_Û）检出效用平台，且
   平台后窗口内 Ŝ（熵估计，flow 主口径）下降幅度超过种子间噪声带
   （5 种子平台后 Ŝ 变化量的标准差 × 2）。
2. S2（世界模型）：四诊断图（plan_v2 第 6 节）中，图一（V̂ 地形梯度
   分位差）超过 CAL-P3 偏差地板换算的最小可检测差，且图二（Î–V̂ 散点，
   含体积校正、flow 密度）线性拟合斜率为正且 R² 高于同散点打乱标签
   零假设的 q95。
3. 无估计器伪影标志：密度双路（flow 对 标准化 kNN）给出的 Ŝ 在开发族
   状态分布上分歧 < 两路各自自检容差之和；支撑度低支撑段占比 < 10%
   （thresholds_v1.support_exclusion 公式）。

## 分支二 · 先修估计器

触发格局（任一）：

- 校准阶段任何一环恢复不了已知真值（仪器门失败，方案 v2 第 5 节第 5 条）。
- 描述阶段密度双路 Ŝ 分歧 ≥ 两路自检容差之和（上条第 3 款反面）。
- S1 的 Î–V̂ 散点呈量程压缩签名（对 k ∈ {16, 32, 64} 的 V̂ 重算使拟合
  斜率漂移超过 30%）：触发 S1 的 T̂ 记不可辨识（降级预案，
  results/calibration/vhat_debias/DECISION.md 预置路径），S1 支线按
  plan_v2 第 7 节第 4 条降级后继续，不阻塞整体。
- 支撑度低支撑段占比 ≥ 10%：修参照集规模或采样后重测。

### 校准阶段执行记录（M1 内已发生的分支二事件，追溯备案）

1. 2026-08-24 触发：CAL-P4 全链未恢复真温度（kNN 密度路，
   results/calibration/cal_p4/20260824-080333/）。
2. 同日修复闭环：flow 密度路使主配置 T̂ 误差降至 1.2–6.0%，隔离诊断
   排除偏差抵消（results/calibration/cal_p4/20260824-081859/）。
3. 残余：V̂ 在集中各向异性几何上的量程压缩未闭合，S1 支线 T̂ 预置
   降级路径（results/calibration/vhat_debias/DECISION.md）；触发与否
   以描述阶段实测为准。

## 分支三 · 停止或转证伪短文

触发格局（任一，跨开发种子稳定复现指 ≥4/5 种子同向）：

- 效用平台后 ⟨V̂⟩（平均势估计）上升，跨开发族种子稳定复现。
- Î–V̂（自信息对势）散点无结构（斜率与零差异不显著且 R² 低于打乱
  标签零假设 q95），且体积校正消融不改变该判断。
- S2 观测轨迹对度量口径主代理的 Â_OM 分布重叠度高到集中性不可分辨
  （标准化间隙中位 < CAL-P2 空白地板二项 q95 对应值），且 V̂ 地形梯度
  分位差落在噪声地板内——推理尺度两预测（P2 低作用量、P3 近测地）
  同时不可检，转规模缩减或证伪路线，转向前须与用户商定。

## 签署

填写人：Claude（代理执行；三分支判据由校准产物导出，用户可在任务六
启动前修订本页，修订须在 git 历史留痕）
日期：2026-08-24
提交 hash：见本文件所在提交（git log --follow docs/precommitment_template.md）
